1. Add .env and refer to the .env the service/api.ts IP address used to connect to the backend
2. Expose the backend API and find way to create a static domain name so the app dont need to be reinstall when the IP of the backend changes.
3. Use CloudFlare tunnel for creating the static domain
4. Use Porkbun / CloudFlare Registrar to buy a domain name for the project